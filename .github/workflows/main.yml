# This workflow is kept for GitHub Advanced Security configuration reference.
# Actual CodeQL analysis runs in codeql.yml.
#
# All PR component builds run in ci.yml (path-filtered).
# All service publish pipelines run in main-*.yml (on push to main).
